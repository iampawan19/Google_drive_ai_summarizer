# ==============================================================================
# DJANGO CONFIGURATION
# ==============================================================================

# Django secret key for cryptographic signing
# Generate a new one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-secret-key-here

# Debug mode - set to False in production
DJANGO_DEBUG=True

# ==============================================================================
# FASTAPI SERVICE
# ==============================================================================

# FastAPI service URL (where the AI service is running)
# Note: FastAPI runs on port 8001, Django runs on port 8000 (default)
FASTAPI_SERVICE_URL=http://127.0.0.1:8001

# ==============================================================================
# GOOGLE DRIVE API
# ==============================================================================

# Path to Google Drive OAuth credentials JSON file
# Download from: https://console.cloud.google.com/apis/credentials
GOOGLE_DRIVE_CREDENTIALS_PATH=credentials.json

# Path to store OAuth token (auto-generated after first authentication)
GOOGLE_DRIVE_TOKEN_PATH=token.pickle

# ==============================================================================
# OPENAI API
# ==============================================================================

# OpenAI API key for text summarization
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model to use for summarization
# Options: gpt-3.5-turbo, gpt-4, gpt-4-turbo-preview
OPENAI_MODEL=gpt-3.5-turbo

# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
# 
# 1. Copy this file to .env in the project root
#    cp .env.example .env
#
# 2. Fill in all the required values above
#
# 3. Set up Google Drive API:
#    - Go to https://console.cloud.google.com/
#    - Create a new project or select existing one
#    - Enable Google Drive API
#    - Create OAuth 2.0 credentials (Desktop app)
#    - Download credentials.json to project root
#
# 4. Get OpenAI API key:
#    - Go to https://platform.openai.com/api-keys
#    - Create a new secret key
#    - Copy and paste it above
#
# 5. Run the application:
#    - Start FastAPI: uvicorn ai_service.main:app --reload
#    - Start Django: python manage.py runserver
#
# ==============================================================================
